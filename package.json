{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --hoist && npm run build",
    "postci": "lerna bootstrap --hoist --ci && npm run build",
    "build": "lerna run build",
    "test": "lerna run test",
    "publish:fake": "lerna run publish --skip-npm --conventional-commits"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}